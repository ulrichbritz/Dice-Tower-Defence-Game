using UnityEngine;

namespace UB
{
    public class EnemyStateManager : StateManager
    {
        protected override void Update()
        {
            base.Update();
        }

        protected override void RunStateMachine()
        {
            base.RunStateMachine();
        }

        public override void SwitchToNextState(State nextState)
        {
            base.SwitchToNextState(nextState);
        }
    }
}
